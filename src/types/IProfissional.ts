export default interface IProsfissional {
    id: number,
    imagem: string,
    nome: string,
    especialidade: string,
    nota: number
}

// estamos declarando que  o objeto profissional qunado recebermos ele, irá ter essas informações e nesses formatos.